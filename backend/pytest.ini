[pytest]
# Make "import app" work when pytest is launched from backend/ without installing the package.
pythonpath = .
testpaths = tests
markers =
    model: loads the real embedding models (slow, ~1.5 GB of RAM, needs them in the Hugging Face cache); tests/conftest.py warns and checks the available memory before running them
