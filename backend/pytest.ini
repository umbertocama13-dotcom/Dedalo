[pytest]
# Make "import app" work when pytest is launched from backend/ without installing the package.
pythonpath = .
testpaths = tests
markers =
    model: loads the real embedding model (slow, needs it in the Hugging Face cache); skip with -m "not model"
