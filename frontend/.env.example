# Copy to .env to override the defaults. Never commit .env.
# VITE_* variables are embedded in the JavaScript sent to the browser:
# they are public, never put secrets here.

# Backend base URL (default: http://127.0.0.1:8000)
VITE_API_URL=http://127.0.0.1:8000
